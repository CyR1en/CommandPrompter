plugins {
    alias(libs.plugins.shadow)
    alias(libs.plugins.paperweight)
}

dependencies {
    paperweight.paperDevBundle("1.21.8-R0.1-SNAPSHOT")
}

shadowJar {
    archiveBaseName.set('CommandPrompter-Sign-Wrapper')
    archiveClassifier.set('')
}

artifacts {
    archives shadowJar
}